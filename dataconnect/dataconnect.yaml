specVersion: "v1beta"
serviceId: "quiki"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "quiki-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
